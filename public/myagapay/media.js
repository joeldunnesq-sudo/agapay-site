function mediaEscape(value) { return String(value ?? '').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;'); }
function mediaDate(value) { const date = new Date(value || ''); return Number.isNaN(date.getTime()) ? '' : date.toLocaleDateString(undefined,{month:'short',day:'numeric',year:'numeric'}); }
function mediaDuration(seconds) { const n=Math.max(0,Math.round(Number(seconds)||0)), h=Math.floor(n/3600), m=Math.floor((n%3600)/60), s=n%60; return h?`${h}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`:`${m}:${String(s).padStart(2,'0')}`; }
function nativeCard(item, featured=false) { const href=`/myagapay/media/watch?video=${encodeURIComponent(item.id)}`; return `<a class="${featured?'media-feature':'media-card'}" href="${href}"><span class="media-thumbnail"><img src="${mediaEscape(item.thumbnailUrl)}" alt="" loading="${featured?'eager':'lazy'}" /><em>${mediaDuration(item.durationSeconds)}</em><i aria-hidden="true">▶</i></span><span class="media-card-copy">${featured?'<small>Featured parish video</small>':''}<strong>${mediaEscape(item.title)}</strong><span>${mediaEscape(mediaDate(item.publishedAt))} · ${Number(item.watchCount||0)} parishioner${Number(item.watchCount||0)===1?'':'s'} watched</span>${featured?`<p>${mediaEscape(item.description||'')}</p>`:''}</span></a>`; }
function youtubeCard(item) { return `<a class="media-card youtube-card" href="${mediaEscape(item.youtubeUrl)}" target="_blank" rel="noopener noreferrer"><span class="media-thumbnail"><img src="${mediaEscape(item.thumbnailUrl)}" alt="" loading="lazy" /><i class="youtube-play" aria-hidden="true">▶</i></span><span class="media-card-copy"><small>Public on YouTube ↗</small><strong>${mediaEscape(item.title)}</strong><span>Leaves AGAPAY · Public video</span></span></a>`; }
async function loadMedia() { const status=document.getElementById('mediaStatus'); try { const response=await fetch('/api/donor/videos',{headers:window.MyAgapayShell?.authHeaders()||{},cache:'no-store'}); if(window.MyAgapayShell?.handleUnauthorized(response)) return; const data=await response.json(); if(!response.ok) throw new Error(data.error||'Unable to load Media.'); document.getElementById('mediaParishName').textContent=data.parish?.name||'Your parish'; const videos=data.videos||[], youtube=data.youtube||[]; document.getElementById('mediaSkeleton').hidden=true; status.hidden=true; if(videos.length){document.getElementById('nativeMediaSection').hidden=false; document.getElementById('mediaFeatured').innerHTML=nativeCard(videos[0],true); document.getElementById('mediaGrid').innerHTML=videos.slice(1).map(item=>nativeCard(item)).join('')||'<p class="media-empty">More parish video will appear here.</p>';} else {status.hidden=false; status.textContent='No private parish videos have been published yet.';} if(youtube.length){document.getElementById('youtubeMediaSection').hidden=false; document.getElementById('youtubeMediaGrid').innerHTML=youtube.map(youtubeCard).join('');} } catch(error){document.getElementById('mediaSkeleton').hidden=true; status.hidden=false; status.textContent=error.message||'Unable to load Media.';} }
document.addEventListener('DOMContentLoaded',loadMedia);
