{
  "name": "agapay-site",
  "version": "0.1.0",
  "private": true,
  "description": "AGAPAY Orthodox giving platform frontend and API foundation.",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node --check src/worker.js && node scripts/check.mjs && node scripts/check-learn.mjs && node scripts/worker-hardening-tests.mjs",
    "prelaunch": "node scripts/prelaunch-checks.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
