{
  "name": "agapay-site",
  "version": "0.1.0",
  "private": true,
  "description": "AgaPay Orthodox giving platform frontend and API foundation.",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node --check src/worker.js && node scripts/check.mjs && node scripts/worker-hardening-tests.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
